import styled from "styled-components";

export const StyledCardHeader = styled.header`
  width: 90%;
  display: flex;
  gap: 2.4rem;
  font-size: 2.8rem;
  font-weight: 600;
  
  button {
    position: absolute;
    top: 2rem;
    right: 2rem;
    cursor: pointer;
  }

  button svg {
    font-size: 2.8rem;
  }
`